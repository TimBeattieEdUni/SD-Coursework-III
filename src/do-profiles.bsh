#!/bin/bash

# Important: This script does not work on the CPLABS machines!

# Runs program twice using Python's profiler to produce lists of functions which
# are called most often and which take the most time.

python -m cProfile tsp.py 10 citiesAndDistances.pickled test-tmp.pickled 12345 > tsp.profile
